답변형 게시판 (HTML + JS)

사용법
- 브라우저에서 `index.html`을 열면 동작합니다.
- 새 글 버튼으로 작성, 각 글에서 `답글`, `수정`, `삭제` 사용 가능.
- 게시물 데이터는 로컬스토리지에 저장됩니다 (KEY: `qa_posts_v1`).

구성
- index.html: UI 마크업
- styles.css: 스타일
- script.js: 클라이언트 로직 (로컬 저장)

원한다면 다음을 추가해 드립니다:
- 글 작성 시 모달, 이미지 업로드, 마크다운 지원
- 서버 연동 (REST API)
